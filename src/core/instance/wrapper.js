/* /src/core/instance/wrapper */

import {
    typeOf
} from '../../utils/tool';

export default function initWrapper(jc){
    const {
        el,
        cropperWidth,
        cropperHeight
    } = jc;

    if(typeOf === 'string'){

    }
}
